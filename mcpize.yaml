# MCPize Configuration
# Docs: https://mcpize.com/docs/mcpize-yaml

version: 1

name: "Deep Research"
slug: "deep-research-99"

runtime: container

build:
  dockerfile: "Dockerfile"

start:
  type: stdio
  command: "node build/index.js"
  args:
    - "build/index.js"

bridge:
  mode: stdio
