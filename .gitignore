# Keep build ignored locally, but Dockerfile builds it in cloud
node_modules/
dist/
.env
.env.local
.DS_Store
*.log
.mcp-deep-research/
cache/
coverage/
.idea/
.vscode/
*.tsbuildinfo
# Don't ignore build for Docker? Actually keep ignored - Docker builds fresh
build/
